<div id="message-<%= message.id %>">
  <small>
    <strong><%= User.find(message.user_id).random_nickname %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
