<div class="landing-page-general-div">
  <h1>Next Stop</h1>
  <p>Chaque bonne journée commence par un bon trajet : sociabilisez facilement dans les transports en commun</p>
  <% if user_signed_in? %>
    <%= link_to new_trip_session_path do %>
      <button class="white-btn" type="button">Entrer</button>
    <% end %>
  <% else %>
    <%= link_to new_user_session_path do %>
      <button class="white-btn" type="button">Entrer</button>
    <% end %>
  <% end %>
</div>
